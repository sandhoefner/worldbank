#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../topojson/bin/topojson-svg" "$@"
  ret=$?
else 
  node  "$basedir/../topojson/bin/topojson-svg" "$@"
  ret=$?
fi
exit $ret
